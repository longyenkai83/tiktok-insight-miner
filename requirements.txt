# Pinned versions — snapshot 2026-07-14
# Tested on Python 3.13 (local Windows) + Python 3.11-slim (Railway Docker Linux).
# Chỉ pin direct dependencies; transitive deps để pip tự resolve.
# Khi cần nâng version: đổi số, test local, push Railway rebuild để verify.

anthropic==0.96.0
apify-client==2.5.0
pydantic==2.13.3
python-dotenv==1.2.2
streamlit==1.57.0
openpyxl==3.1.5
google-api-python-client==2.198.0
google-auth==2.56.0
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0
